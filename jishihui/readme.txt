1.首页在src的文件夹下面的index.html
2.运行在php服务器环境下，首页目录:localhost
3.show文件夹中放的压缩和转译过的文件和sass文件。模块化为空。
4.无默认用户名密码
5.index.html是首页，数据渲染在1F右边那六张图。头部有登录，注册，注销。
denglu.html是登录页面，zhuce.html是注册页面。注册，登录之后会存储一条localstorage。
shop.html是商品详情页面，实现了放大镜功能。
car.html是购物车页面，可以读取点击加入购物车时增加的cookie，在页面上显示出来。
6.不足：没有写商品列表的页面。没有实现登录后才能使用购物车的功能。渲染的数据只能跳转到写好的页面，不能根据点击的数据跳转到相应的页面。没有实现功能的模块化。
7.原网页已被毁尸灭迹。（原网页已升级为app版本，网页打不开了。）